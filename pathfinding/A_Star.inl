#include <algorithm>

namespace app {
    template <size_t gridWidth, size_t gridHeight>
    void A_Star::findPath(Grid<gridWidth, gridHeight>& grid, const app::vec2<int>& star, const app::vec2<int>& end) {

    }
}